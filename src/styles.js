import {css} from "lit";

export const basicStyles = css`
        :host {
            display: block;
            padding: 16px;
            color: var(--my-element-text-color, black);
            font-family: Arial, sans-serif;
        }

        h1 {
            color: #035400;
        }
    `;
